@page "/memos"
@inject IMemoService service

<VccGrid Records=@memos AllowToAdd="true" CreationPage="/memos/create" >
    <ItemHeader>
        <MemoDt Mode="DetModes.GridHeader"/>
    </ItemHeader>
    <ItemRow Context="memo">
        <MemoDt Entity=memo Mode="DetModes.GridView"/>
    </ItemRow>
</VccGrid>

@code {
    List<Memo>? memos;

    protected override void OnInitialized()
    {
        memos = service?.GetAll() ?? new List<Memo>();
    }
}